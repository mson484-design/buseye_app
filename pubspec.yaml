name: buseye_app
description: "BusEye In-Vehicle Safety System"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  flutter_tts: ^3.8.3
  geolocator: ^10.1.0
  sensors_plus: ^7.1.0  # 👈 이 부분을 7.1.0으로 변경

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
